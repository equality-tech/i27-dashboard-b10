name: env var demo
on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    
    steps:
      - name: job1 step1 statements
        run: |
          echo "======================"
          echo "ORG_NAME name is ${{vars.ORG_NAME}}"     
          echo "ORG_PASSWORD name is ${{secrets.ORG_PASSWORD}}"   
          
          echo "REPO_NAME name is ${{vars.REPO_NAME}}"     
          echo "REPO_SECRETE name is ${{secrets.REPO_SECRETE}}"  
          echo "======================"               
      
  second-job:
    runs-on: ubuntu-latest
    steps:
      - name: job2 step1 statements      
        run: |
          echo "======================"
          echo "ORG_NAME name is ${{vars.ORG_NAME}}"     
          echo "ORG_PASSWORD name is ${{secrets.ORG_PASSWORD}}"           
          echo "REPO_NAME name is ${{vars.REPO_NAME}}"     
          echo "REPO_SECRETE name is ${{secrets.REPO_SECRETE}}"  
          echo "======================"               
