name: env var demo
on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    
    steps:
      - name: job1 step1 statements
        run: |
          echo "======================"
          echo "APP_NAME=my-demo-app">>"$GITHUB_ENV"
          echo "$APP_NAME"      
          echo "======================"               
      - name: job1 step2 steps
        run: |
          echo "======================"
          echo "$APP_NAME"        
          echo "======================"  

  second-job:
    needs: first-job
    runs-on: ubuntu-latest
    
    steps:           
      - name: job2 step1 steps
        run: |
          echo "======================"
          echo "$APP_NAME"        
          echo "======================"  
